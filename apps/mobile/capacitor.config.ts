import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bloodstock.app',
  appName: 'Bloodstock',
  webDir: '../../dist/apps/mobile',
  bundledWebRuntime: false,
};

export default config;
